import 'package:flutter/material.dart';

class LuaCardWidget extends StatelessWidget {
  const LuaCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
