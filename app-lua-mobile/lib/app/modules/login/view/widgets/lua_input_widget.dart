import 'package:flutter/material.dart';

class LuaInputWidget extends StatelessWidget {
  const LuaInputWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
