import 'package:flutter/material.dart';

class LuaButtonWidget extends StatelessWidget {
  const LuaButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
